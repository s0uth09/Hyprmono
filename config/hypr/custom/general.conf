# Personal overrides go here.
# This file is sourced last and is never overwritten by future updates to
# the main config. (Native re-implementation of the dynamic stub-file
# creation previously done at runtime by services/custom_config.lua —
# native Hyprland config has no scripting, so the stub files are now
# shipped statically instead of generated on first launch.)
