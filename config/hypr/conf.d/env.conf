# env.conf — environment variables
# Native translation of hyprland/env.lua. hl.env(k, v) -> env = k,v

env = HYPRCURSOR_THEME,rose-pine-hyprcursor
env = HYPRCURSOR_SIZE,32
env = XCURSOR_SIZE,32
env = XDG_CURRENT_DESKTOP,Hyprland
